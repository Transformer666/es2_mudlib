// Room: /d/fengcheng/fortune.c

inherit ROOM;

void create()
{
	set("short", "神算子卦攤");
	set("long", @LONG
這是風城廣場東側的一座卦攤。一張舊木桌上鋪著洗得發白的青
布﹐攤前豎著一面同樣青布的布幡﹐上書「神算子」三個大字﹐底下
一行小字寫著﹕「鐵口直斷﹐窮通禍福﹐天賦根器﹐一算便知」。桌
上擺著一副磨得油亮的龜甲銅錢、一個籤筒、一方端硯﹐還壓著幾張寫
滿生辰八字的黃紙。攤後坐著一位仙風道骨的算命先生﹐閉目養神﹐自
有一股高深莫測的氣度。攤前圍著幾個求問前程的人﹐聽得連連點頭。
往西可以回到風城廣場。
LONG
	);
	set("detail", ([
		"布幡" : "那面青布布幡上「神算子」三字寫得龍飛鳳舞﹐底下小字寫著「鐵口直斷﹐窮通禍福﹐天賦根器﹐一算便知」﹐口氣大得很。\n",
		"龜甲" : "桌上一副龜甲銅錢已被摩挲得油亮﹐據說那位先生便是以此卜算吉凶﹐拋擲之間﹐窮通禍福了然於胸。\n",
		"籤筒" : "竹製的籤筒裡插著一支支竹籤﹐求問的人虔心搖出一支﹐先生便依籤上的字句斷你的前程。\n",
		"黃紙" : "桌上壓著幾張寫滿生辰八字的黃紙﹐都是方才求問過的人留下的﹐字跡潦草﹐想是先生掐算時隨手記下的。\n",
		"端硯" : "一方溫潤的端硯擺在桌角﹐硯中墨汁未乾﹐先生時而提筆在黃紙上寫畫幾筆﹐算是替人批命。\n",
	]));
	set("objects", ([
		__DIR__"npc/fortuneteller" : 1,
	]));
	set("outdoors", "fengcheng");
	set("no_fight", 1);
	set("exits", ([ /* sizeof() == 1 */
		"west" : __DIR__"plaza",
	]));

	setup();
	replace_program(ROOM);
}

// vim: set ts=4 sw=4 syntax=lpc

// sky_eye.c -- 正史主線第八章「天靈任務」天靈伏誅所掉的信物：天靈之眼。
//
// 來歷（承接 canon 主線樹 docs 05 L59-60「天靈任務(赤魈村許願池) → 天命刃(dagger of
//   fate) + eye」；江隕第八章交差所伏「往赤魈村許願池解天命刃與一枚『眼』」之 lead）：
//   赤魈村後那一泓終年不涸的許願池，相傳是天地靈氣所鍾、上應天靈之所。少俠以渾沌之
//   心、星光環引動池水，將那盤踞池上、司一方天命的天靈招出而降——自牠那一點未散的靈
//   光中，凝出這一枚通體流轉著澄澈天光的『天靈之眼』。此眼既是了結天靈的鐵證、亦是
//   日後往京畿神社獻與日月神薛的三信物(心、眼、星光環)之一。
//
// 用法（純任務信物，不另設機制）：
//   * 持此眼往京畿神社獻與日月神薛(Ch.9)，與『地靈之心』、『星光環』同獻，為再戰侮
//     天鬼的終局之路鋪墊；亦為漕幫江隕第八章交差覆命的信物之一。
//   * ids 兼收英文 "sky eye"/"eye" 與中文「天靈之眼」。
//   設 no_sell：主線信物，玩家須帶在身邊，斷不可變賣。
//
// 注意：本眼為天靈 die() 逐玩家掉落的任務信物（每位 tianling-slayer 各掉一枚），不
//   inherit F_UNIQUE（鏡 chaos_heart/seal_fire 之做法：僅以 set("unique",1) 作風味標
//   記，免多人持眼時的任何意外自毀）。

#include <ansi.h>

inherit ITEM;

void create()
{
	set_name(HIC "天靈之眼" NOR,
		({ "sky eye", "eye", "天靈之眼" }) );
	set_weight(400);

	if( !clonep() ) {
		set("unit", "枚");
		set("value", 1);
		set("unique", 1);
		set("no_sell", 1);
		set("long",
			"一枚自天靈靈光所凝、通體流轉著澄澈天光的奇眼。眼形渾圓如\n"
			"珠，既非金石、亦非玉璧，而似一汪將凝未凝的天地靈氣所鑄——握\n"
			"之在手，但覺一縷清越而高遠的天光自掌心緩緩漫開，眼瞳深處有\n"
			"星河微芒明滅迴盪，俯仰之間，彷彿能照見一方天命的去從。這便\n"
			"是赤魈村許願池上那司一方天命的天靈伏誅之際，自牠那一點未散\n"
			"的靈光中凝出的『天靈之眼』了。傳聞此眼須與『地靈之心』、那\n"
			"枚星光環同獻於京畿神社的日月神薛，再戰侮天鬼的終局之路，方\n"
			"能豁然在望。\n");
	}
	setup();
}

// vim: set ts=4 sw=4 syntax=lpc

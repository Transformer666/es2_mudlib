// Room: /d/wutang/ferry.c
//
// 五堂鎮過河渡口。docs 主線疑團「過河的船在哪裡？」(docs/01-世界觀與劇情/
//   05-主線任務與NPC.md:32；地圖 04-地圖與場景.md:14「過河船」)。
//   docs 未指出對岸已建何區、亦未給「船怎麼來」之解答，故本房做渡口場景 +
//   船夫 NPC(npc/boatman.c)，由船夫對白伏筆「水大暫不擺渡 / 船被借走」——
//   不臆造對岸區域、不開實際傳送(對岸區建好後再接 boatman 的擺渡邏輯，caveat)。
//
// 鐵則遵循：
//   #11：本房有自訂 init()/do_search()(伏筆)，**不可** replace_program(ROOM)。
//
// 範式來源：d/snow/ruin1.c(自訂 init+do_search)、d/wutang/eastlane.c(村景房)。

inherit ROOM;

int searched = 0;

void create()
{
	set("short", "過河渡口");
	set("long", @LONG
出了雜貨鋪後門﹐順著一道青石砌的緩坡而下﹐便是五堂鎮西邊的
過河渡口。一條寬闊的大河(river)橫亙在此﹐河水近日漲得湍急﹐濁浪
拍打著河岸﹐捲起一團團黃濁的泡沫﹐對岸的景物都籠在一層水霧裡看
不真切。岸邊立著一根繫船的木樁﹐樁上的纜繩(rope)空盪盪地垂在水
面﹐卻不見渡船的影子。一個老船夫蹲在岸邊的礁石上﹐叼著旱菸袋﹐
望著滔滔的河水出神。看來這當口﹐是過不得河了。北邊的緩坡通回鎮
上的雜貨鋪。
LONG
	);
	set("detail", ([
		"大河" : "這條大河寬有數十丈﹐近日連場大雨﹐上游的水都匯了下來﹐河面漲得又寬又急﹐渾濁的浪頭打著旋兒往下游捲去﹐尋常的舟子這時節是斷不敢下水的。隔著茫茫水霧﹐依稀可見對岸有山影綽綽﹐卻看不分明那是何處去界。\n",
		"river" : "這條大河寬有數十丈﹐近日連場大雨﹐上游的水都匯了下來﹐河面漲得又寬又急﹐渾濁的浪頭打著旋兒往下游捲去﹐尋常的舟子這時節是斷不敢下水的。隔著茫茫水霧﹐依稀可見對岸有山影綽綽﹐卻看不分明那是何處去界。\n",
		"纜繩" : "繫船的纜繩空盪盪地垂在木樁上﹐繩頭還是濕的——看來那渡船離岸並沒有多久﹐卻不知是被大水沖走了﹐還是教甚麼人借了去。\n",
		"rope" : "繫船的纜繩空盪盪地垂在木樁上﹐繩頭還是濕的——看來那渡船離岸並沒有多久﹐卻不知是被大水沖走了﹐還是教甚麼人借了去。\n",
		"木樁" : "岸邊一根碗口粗的繫船木樁﹐被纜繩勒出一圈圈深深的痕跡﹐想是這渡口已擺渡了不知多少年月。\n",
		"水霧" : "對岸籠在一層茫茫的水霧裡﹐霧中山影綽綽﹐卻看不分明——鎮上的老人說﹐霧那頭的去處﹐近年來已少有人過去了。\n",
	]));
	set("outdoors", "wutang");
	set("no_fight", 1);
	set("objects", ([
		__DIR__"npc/boatman" : 1,
	]));
	set("exits", ([
		"north" : __DIR__"store",
	]));

	setup();
}

void init()
{
	add_action("do_search", "search");
}

// search 河岸/纜繩 → 伏筆「船的下落」。只給 lore、不臆造擺渡/對岸獎勵。
int do_search(string arg)
{
	object me = this_player();

	if( arg != "河岸" && arg != "纜繩" && arg != "rope" && arg != "木樁" )
		return notify_fail("你要搜查甚麼﹖（試試 search 河岸）\n");

	if( !me ) return 0;

	if( searched ) {
		message_vision(
			"$N又沿著泥濘的河岸尋了一圈﹐除了那道深深的拖痕﹐"
			"再看不出渡船究竟去了哪裡。\n", me);
		return 1;
	}
	searched = 1;
	message_vision(
		"$N蹲下身﹐在濕滑的河岸泥地上細看——岸邊的爛泥裡留著一道極深"
		"的拖痕﹐像是有人費了大力把渡船硬生生拖離了水面、拖向下游的蘆葦"
		"叢裡藏了起來﹐拖痕旁還有幾枚雜沓的腳印﹐早被漲上來的河水泡得"
		"模糊了。這渡船﹐分明不是被大水沖走的——是有人﹐刻意把它藏了。\n", me);
	me->gain_score("explorer fame", 5);
	return 1;
}

void reset()
{
	searched = 0;
	::reset();
}

// vim: set ts=4 sw=4 syntax=lpc

// Room: /d/wutang/templeinner.c
//
// 鎮天神廟內殿。docs 疑團「鎮天神廟內藏著誰？」之伏筆所在——廟內「藏」著的人
//   即枯守此處的老廟祝(隱者)司天緣(見 npc/miaozhu.c)。docs 未給謎底，故老廟祝
//   只給 lore 對白與伏筆 hook(ask about 神君/封印/藏著誰)，不開完整解謎/獎勵。
//
// 鐵則遵循：
//   #11：本房有自訂 init()/do_search()(伏筆)，**不可** replace_program(ROOM)。
//   #5 ：直接 inherit ROOM、自訂 init() 只 add_action、setup() 走 ROOM::setup()。
//
// 範式來源：d/snow/ruin1.c(自訂 init()+do_search、不 replace_program)、
//           d/snow/npc/heiyilaoren.c(伏筆 NPC 入 objects)。

inherit ROOM;

int searched = 0;

void create()
{
	set("short", "神廟內殿");
	set("long", @LONG
這是鎮天神廟後的內殿﹐比前殿更顯幽暗逼仄。四壁的牆上繪著早
已斑駁的壁畫(mural)﹐畫的依稀是一場慘烈的征伐——刀光劍影裡﹐一
位按劍的神將正將甚麼龐然的兇物鎮入地底。殿角一張蒲團上﹐枯坐著
一個鬚眉皆白的老廟祝﹐他垂目誦經﹐對外間的喧囂恍若未聞﹐彷彿這
殿裡的歲月與外頭是兩重天地。內殿深處一道緊閉的石門(door)封著厚
厚的塵封﹐門上一道泛黃的封條﹐隱隱透出一股說不出的森冷。一道側
階(down)通回前殿。
LONG
	);
	set("detail", ([
		"壁畫" : "四壁的壁畫已剝落大半﹐勉強能拼湊出一段舊事﹕一位按劍的神將立於陣前﹐身後是漫天的香火與祈禱的眾生﹐他正將一頭遮天蔽日、面目模糊的兇物﹐一寸寸地鎮入裂開的地縫之中。壁畫的末端﹐神將的腳下題著一行小字﹕「鎮之於此﹐永鎮天下太平」——可那兇物的眉眼處﹐卻被後人用利器狠狠刮去了﹐像是有人不願它被認出。\n",
		"mural" : "四壁的壁畫已剝落大半﹐勉強能拼湊出一段舊事﹕一位按劍的神將立於陣前﹐身後是漫天的香火與祈禱的眾生﹐他正將一頭遮天蔽日、面目模糊的兇物﹐一寸寸地鎮入裂開的地縫之中。壁畫的末端﹐神將的腳下題著一行小字﹕「鎮之於此﹐永鎮天下太平」——可那兇物的眉眼處﹐卻被後人用利器狠狠刮去了﹐像是有人不願它被認出。\n",
		"石門" : "內殿深處那道石門足有半尺厚﹐門縫裡透出一絲沁骨的森冷。門上斜貼著一道泛黃的硃砂封條﹐封條上的符籙早已模糊﹐卻仍隱隱有股壓人的氣機。老廟祝說﹐這門後鎮著的東西﹐萬萬開不得。\n",
		"door" : "內殿深處那道石門足有半尺厚﹐門縫裡透出一絲沁骨的森冷。門上斜貼著一道泛黃的硃砂封條﹐封條上的符籙早已模糊﹐卻仍隱隱有股壓人的氣機。老廟祝說﹐這門後鎮著的東西﹐萬萬開不得。\n",
		"封條" : "那道硃砂封條年深日久﹐符文盡褪﹐卻教人莫名地不敢去揭。湊近細看﹐封條一角還壓著半枚小小的官印泥痕——這封禁的物事﹐莫非連朝廷也曾插手過﹖\n",
		"蒲團" : "殿角一張磨得發亮的舊蒲團﹐想是老廟祝日日枯坐誦經之處﹐蒲團邊上的青磚﹐已被他的膝頭壓出兩個淺淺的凹痕。\n",
	]));
	set("objects", ([
		__DIR__"npc/miaozhu" : 1,
	]));
	set("no_fight", 1);
	set("exits", ([
		"down" : __DIR__"temple",
	]));

	setup();
}

void init()
{
	add_action("do_search", "search");
}

// search 石門/壁畫 → 伏筆。只給 lore、不給解謎獎勵(石門後鎮著誰、何時可開＝未定)。
int do_search(string arg)
{
	object me = this_player();

	if( arg != "石門" && arg != "door" && arg != "壁畫" && arg != "mural"
	 && arg != "封條" )
		return notify_fail("你要搜查甚麼﹖（試試 search 石門）\n");

	if( !me ) return 0;

	if( searched ) {
		message_vision(
			"$N又在那道石門前徘徊良久﹐封條依舊森冷﹐門後依舊死寂﹐"
			"那壓人的氣機教人不敢造次。\n", me);
		return 1;
	}
	searched = 1;
	message_vision(
		"$N壯起膽子湊近那道封了塵封的石門﹐將耳朵貼上冰冷的石面——"
		"剎那間﹐$N彷彿聽見門後極深極遠處﹐有一線若有若無的低吟﹐"
		"似哭似笑﹐似怨似嘲﹐教人遍體生寒。$N猛地退開﹐回頭去看那"
		"蒲團上的老廟祝﹐老人不知何時已停了誦經﹐正睜著一雙古井無波"
		"的老眼﹐靜靜地望著$N﹐緩緩搖了搖頭。\n", me);
	me->gain_score("explorer fame", 8);
	return 1;
}

void reset()
{
	searched = 0;
	::reset();
}

// vim: set ts=4 sw=4 syntax=lpc

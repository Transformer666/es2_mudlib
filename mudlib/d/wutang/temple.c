// Room: /d/wutang/temple.c
//
// 鎮天神廟前殿。docs 主線疑團「鎮天神廟內藏著誰？」(docs/01-世界觀與劇情/
//   05-主線任務與NPC.md:30；地圖 04-地圖與場景.md:14,112「鎮天神廟 — 五堂鎮謎題」)
//   之 lore 探索房。docs 只列疑團、未給解答，故本房只做伏筆鋪陳(look/search
//   神像/匾額給劇情線索)，不臆造完整解謎/獎勵——謎底留作日後設計決策(caveat)。
//
// 鐵則遵循：
//   #11：本房有自訂 init()/do_search()(伏筆)，**不可** replace_program(ROOM)。
//   #5 ：本房直接 inherit ROOM(非 TEMPLE base)、自訂 init() 只 add_action，
//        setup() 走 ROOM::setup()，objects 內無 NPC(隱者在內殿 templeinner)。
//
// 範式來源：d/snow/ruin1.c(自訂 init()+do_search() 伏筆、不 replace_program)。

inherit ROOM;

int searched = 0;

void create()
{
	set("short", "鎮天神廟");
	set("long", @LONG
這是五堂鎮上的鎮天神廟。廟宇雖不甚宏闊﹐卻自有一股莊嚴肅穆
的氣象。朱漆的廟門早已斑駁褪色﹐門楣上一方烏木匾額(plaque)題著
「鎮天神廟」四個鐵畫銀鉤的大字﹐筆勢沉雄﹐隱隱透著一股鎮壓邪祟
的威勢。殿中央一尊丈餘高的神像(idol)俯視著芸芸眾生﹐神像前一座
青石香案上插滿了將盡未盡的線香﹐青煙裊裊﹐繚繞在樑間經年不散。
殿內光線昏暗﹐唯神像前一盞長明燈幽幽地亮著﹐將那神像的影子拉得
老長﹐投在斑駁的牆上﹐教人莫名地心生敬畏。一道側階(up)通往廟後
的內殿﹐南邊則是出去的廟門﹐通往路口的茶攤。
LONG
	);
	set("detail", ([
		"匾額" : "那方烏木匾額年深日久﹐「鎮天神廟」四字的金漆早已剝落﹐卻仍掩不住筆鋒裡那股鎮壓的威勢。匾額一角依稀刻著兩行極小的舊款﹐被香煙燻得發黑﹐勉強辨得出「鎮天神君座下」幾字﹐其餘已漫漶難認了。\n",
		"plaque" : "那方烏木匾額年深日久﹐「鎮天神廟」四字的金漆早已剝落﹐卻仍掩不住筆鋒裡那股鎮壓的威勢。匾額一角依稀刻著兩行極小的舊款﹐被香煙燻得發黑﹐勉強辨得出「鎮天神君座下」幾字﹐其餘已漫漶難認了。\n",
		"神像" : "這尊神像以整木雕成﹐外塗金漆﹐法相莊嚴。神像一手按劍、一手結印﹐雙目低垂卻精光內蘊﹐似在俯察人間﹐又似在鎮守著甚麼。細看那神像足下﹐竟踏著一團模糊難辨的形影﹐像是一頭被鎮壓的兇物——廟祝諱莫如深﹐鎮民也說不清那踏著的究竟是何方邪祟。你心頭莫名一凜﹐總覺這廟裡似乎還藏著些不為人知的隱情。\n",
		"idol" : "這尊神像以整木雕成﹐外塗金漆﹐法相莊嚴。神像一手按劍、一手結印﹐雙目低垂卻精光內蘊﹐似在俯察人間﹐又似在鎮守著甚麼。細看那神像足下﹐竟踏著一團模糊難辨的形影﹐像是一頭被鎮壓的兇物——廟祝諱莫如深﹐鎮民也說不清那踏著的究竟是何方邪祟。你心頭莫名一凜﹐總覺這廟裡似乎還藏著些不為人知的隱情。\n",
		"香案" : "神像前一座青石香案﹐案上插滿了長長短短的線香﹐香灰積得老厚﹐顯見這廟雖偏僻﹐香火卻從未斷過。\n",
		"長明燈" : "神像前一盞長明燈幽幽地亮著﹐燈油上似浮著一層說不出的暗光﹐火苗終年不熄﹐廟祝說這燈一旦熄了﹐廟裡鎮著的東西便要不安生。\n",
		"廟門" : "朱漆的廟門斑駁褪色﹐門軸鏽蝕﹐推起來吱呀作響﹐想是許久不曾大修了。\n",
	]));
	set("outdoors", "wutang");
	set("no_fight", 1);
	set("exits", ([
		"south" : __DIR__"teastall",
		"up" : __DIR__"templeinner",
	]));

	setup();
}

void init()
{
	add_action("do_search", "search");
}

// search 神像/香案 → 伏筆鋪陳。只給 lore 線索、不給解謎獎勵(謎底未定)。
int do_search(string arg)
{
	object me = this_player();

	if( arg != "神像" && arg != "idol" && arg != "香案" && arg != "altar" )
		return notify_fail("你要搜查甚麼﹖（試試 search 神像）\n");

	if( !me ) return 0;

	if( searched ) {
		message_vision(
			"$N又繞著神像底座細細搜查了一遍﹐除了那團教人心驚的踏影﹐"
			"再看不出別的端倪。\n", me);
		return 1;
	}
	searched = 1;
	message_vision(
		"$N伏低身子﹐就著長明燈幽微的光﹐細看那神像的底座。香灰積處﹐"
		"竟摸到座下青石上鑿著一圈極淺的古篆﹐被經年的香火燻得幾乎平了。"
		"$N勉強辨出其中幾字﹕「 ... 鎮之以鎮天﹐封之以五堂 ... 待其人歸 ... 」"
		"——後面的字盡被磨去了。這「待其人歸」四字﹐莫非便是這廟裡藏著的隱情﹖"
		"$N心頭疑雲更重﹐卻一時無從解起。\n", me);
	me->gain_score("explorer fame", 5);
	return 1;
}

void reset()
{
	searched = 0;
	::reset();
}

// vim: set ts=4 sw=4 syntax=lpc

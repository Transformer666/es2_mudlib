// Room: /d/wutang/statue.c
//
// 巫山神秘雕像。docs 主線疑團「巫山之中神秘雕像有何秘密？」(docs/01-世界觀與
//   劇情/05-主線任務與NPC.md:28；地圖 04-地圖與場景.md:14,111「巫山神秘雕像 —
//   五堂鎮謎題」)之 lore 探索房。docs 只列疑團、未給解答，故本房只做伏筆鋪陳
//   (look/search 雕像給線索)，不臆造完整解謎/獎勵——秘密留作日後設計決策(caveat)。
//
// 鐵則遵循：
//   #11：本房有自訂 init()/do_search()(伏筆)，**不可** replace_program(ROOM)。
//
// 範式來源：d/snow/ruin1.c(自訂 init()+do_search 伏筆、不 replace_program)。

inherit ROOM;

int searched = 0;

void create()
{
	set("short", "巫山雕像");
	set("long", @LONG
出了僻靜小巷東頭﹐順著一條雜草沒徑的小路﹐便到了鎮東這片荒
僻的山坳。坳中孤零零地矗立著一尊巨大的古雕像(statue)﹐遠望像是
一個盤膝而坐的人形﹐近看卻又似人非人——它通體是一種青黑的怪石所
鑿﹐歷經風雨剝蝕﹐五官早已模糊﹐唯有那盤坐的姿態與雙手結成的古
怪手印﹐千百年來紋絲不動。鎮上的老人都喚它作「巫山雕像」﹐說它
比五堂鎮還老得多﹐打有這鎮子起便在這兒了﹐卻沒人說得清是誰、為
何在此立下這麼一尊鎮邪似的怪物。雕像四周的青草都長得格外稀疏﹐
連蟲鳥也不近前。往西是回鎮的小巷。
LONG
	);
	set("detail", ([
		"雕像" : "這尊雕像約莫兩人來高﹐盤膝端坐﹐青黑的石面被千年風雨蝕得坑坑窪窪。它的面目早已漫漶不清﹐唯獨那一雙結著古怪手印的手﹐刻得極是精細傳神——掌心向外、十指曲張﹐像是在抵禦甚麼﹐又像是在封鎮甚麼。你繞著它細看﹐越看越覺得這姿態眼熟﹐竟與鎮天神廟內殿壁畫上那按劍鎮兇的神將﹐隱隱有幾分相通之處。莫非這雕像與那神廟﹐本是同一樁舊事的兩端﹖\n",
		"statue" : "這尊雕像約莫兩人來高﹐盤膝端坐﹐青黑的石面被千年風雨蝕得坑坑窪窪。它的面目早已漫漶不清﹐唯獨那一雙結著古怪手印的手﹐刻得極是精細傳神——掌心向外、十指曲張﹐像是在抵禦甚麼﹐又像是在封鎮甚麼。你繞著它細看﹐越看越覺得這姿態眼熟﹐竟與鎮天神廟內殿壁畫上那按劍鎮兇的神將﹐隱隱有幾分相通之處。莫非這雕像與那神廟﹐本是同一樁舊事的兩端﹖\n",
		"手印" : "雕像雙手結成的手印古樸奇詭﹐掌心向外、十指曲張﹐絕非尋常佛道兩家的印訣。若有精通法術的高人在此﹐或許看得出這手印封鎮的究竟是何方神聖。\n",
		"青草" : "雕像四周一圈的青草都長得格外稀疏枯黃﹐越近雕像越是寸草不生﹐連飛蟲走鳥也遠遠避開﹐彷彿這石像周遭有股無形的氣機﹐教生靈本能地不敢靠近。\n",
	]));
	set("outdoors", "wutang");
	set("no_fight", 1);
	set("exits", ([
		"west" : __DIR__"eastlane",
	]));

	setup();
}

void init()
{
	add_action("do_search", "search");
}

// search 雕像/底座 → 伏筆。只給 lore 線索、不給解謎獎勵(秘密未定)。
int do_search(string arg)
{
	object me = this_player();

	if( arg != "雕像" && arg != "statue" && arg != "底座" && arg != "base" )
		return notify_fail("你要搜查甚麼﹖（試試 search 雕像）\n");

	if( !me ) return 0;

	if( searched ) {
		message_vision(
			"$N又繞著雕像底座摸索了一圈﹐除了那行被青苔蓋住的殘字﹐"
			"再尋不出別的機關。\n", me);
		return 1;
	}
	searched = 1;
	message_vision(
		"$N撥開雕像底座厚厚的青苔﹐就著天光細看﹐隱約摸到石上鑿著一"
		"行被歲月磨平的古字﹐筆畫已殘缺不全﹕「巫山一脈﹐鎮 ... 之根 ... "
		"印破則 ... 」——後半截早已剝落無存。這「印破則 ... 」四字戛然而止﹐"
		"卻教$N心頭沒來由地一沉。底座一側﹐還留著一個淺淺的、像是要嵌入"
		"甚麼物事的方形凹槽﹐如今空空如也﹐不知該以何物相配。\n", me);
	me->gain_score("explorer fame", 6);
	return 1;
}

void reset()
{
	searched = 0;
	::reset();
}

// vim: set ts=4 sw=4 syntax=lpc

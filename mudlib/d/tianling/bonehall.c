// Room: /d/tianling/bonehall.c -- 幽冥骨殿
//
// 正史主線第二章「混沌珠」的第一處決戰場。自幽冥入口(nether.c)北行﹐便是這座以
// 萬骨堆疊而成的森然殿宇——把持混沌珠、以萬骨煉身的骨屍妖王(npc/skeleton_king.c)
// 盤踞其中。此處為決戰場﹐「不」設 no_fight﹔骨屍妖王不主動撲噬(門禁把關在幽冥
// 入口前﹐由 nether 來路與 abyss::valid_leave 濾去未接任務者)﹐玩家自行 kill 起釁。
// 往南可退回幽冥入口﹔往北一道森然的黑暗甬道通向噬魂窟(鬼王噬魂之所在)。
//
// 門禁（mirror den.c::valid_leave）：往北深入噬魂窟須先破骨屍妖王(quest/main_canon2
//   >= 2)——骨屍妖王煉「身」、鬼王噬魂司「魂」﹐互為表裡﹐不破其身則那甬道為萬骨
//   所鎖、不得通行。由 valid_leave 就 north 方向把關﹔故本室定義 valid_leave 自訂
//   函式﹐「不」replace_program（鏡 den.c 保留函式之做法）。

#include <ansi.h>

inherit ROOM;

void create()
{
	set("short", "幽冥骨殿");
	set("long", @LONG
循著那片慘綠的微光北行﹐眼前的黑暗豁然向兩旁退開﹐現出一
座龐然得望不見頂的森然殿宇。這殿宇竟無一磚一石——通體是由不知多
少枯骨層層堆疊、纏絞而成﹕巨象的頭骨壘成廊柱﹐蟒蛇的椎節盤作梁
拱﹐千百根人骨獸骨交錯嵌砌成這一片嶙峋慘白的殿壁﹐骨縫間盡是滲
著的慘綠屍火﹐幽幽明滅﹐將這死寂的殿堂映得鬼氣森森。殿心一座以
無數頭骨壘成的骨臺之上﹐危坐著一座望之如山的骨塔——那便是把持混
沌珠、以萬骨煉身的骨屍妖王了。牠那兩點眼窩中燃著陰綠的鬼磷﹐俯
視之間﹐一股自累累白骨中熬煉出的亙古死寂沉沉壓下﹐教人脊背發
寒。往南可退回幽冥入口﹐往北一道森然的甬道沒入更深的黑暗。
LONG
	);
	set("detail", ([
		"殿宇" : "這座森然的殿宇通體以枯骨堆砌而成﹐廊柱是巨象的頭骨、梁拱是蟒蛇的椎節﹐殿壁則是千百根人骨獸骨交錯嵌砌——想是那骨屍妖王以萬骨煉身之餘﹐連這巢穴也一併以累累白骨壘就了。骨縫間滲著的慘綠屍火幽幽明滅﹐將滿殿映得鬼氣森森。\n",
		"骨屍妖王" : "殿心骨臺上那座望之如山的骨塔﹐便是以萬骨煉身的骨屍妖王。牠並無血肉之軀﹐通體是由無數枯骨層層堆疊、纏絞而成﹐巨象頭骨作冠﹐千骸交錯為甲﹐骨縫間滲著青黑的屍火。據那骨符鬼篆所載﹐這妖王守混沌珠於幽冥﹐萬骨為甲、尋常刀兵難入其身——奪那至邪之珠﹐須先過牠這一座骨山。\n",
		"屍火" : "骨縫間滲出的慘綠屍火陰寒透骨﹐既非燈燭亦非磷火﹐而是自累累白骨中熬煉出的死氣所化。這滿殿的屍火幽幽明滅﹐看得久了﹐竟覺那一明一滅之間﹐彷彿有無數冤魂在無聲地開闔著嘴。\n",
		"甬道" : "殿宇北面那道森然的甬道沿著骨壁向更深的黑暗延伸﹐甬道口為交錯嵌砌的萬骨所鎖﹐隱隱透出一股比這骨殿更陰冷的死寂。那與骨屍妖王互為表裡的鬼王噬魂﹐想必正盤踞在那甬道盡頭的噬魂窟裡——然這萬骨之鎖﹐怕是非破了那煉身的妖王不能開。\n",
	]));
	set("outdoors", "tianling");
	// 決戰場：不設 no_fight。骨屍妖王不主動攻擊﹐玩家自行 kill 起釁。
	set("objects", ([
		__DIR__"npc/skeleton_king" : 1,
	]));
	set("exits", ([
		"south" : __DIR__"nether",
		// 往北深入噬魂窟須先破骨屍妖王(quest/main_canon2 >= 2)，由 valid_leave 把關。
		"north" : __DIR__"soulpit",
	]));

	setup();
}

// 把關（mirror den.c）：往北深入噬魂窟須先破骨屍妖王(quest/main_canon2 >= 2)——其
// 餘方向(south)及預設門禁悉依 ::valid_leave。回傳 0(notify_fail) 即擋下移動。
int valid_leave(object me, string dir)
{
	// 先走預設門禁邏輯﹔不過則直接擋下。
	if( !::valid_leave(me, dir) ) return 0;

	// 僅就通往噬魂窟的 north 方向設旗標把關﹐其餘方向放行。
	if( dir == "north" && (!me || me->query("quest/main_canon2") < 2) )
		return notify_fail(
			"殿宇北面那道甬道為交錯嵌砌的萬骨層層鎖死﹐並無去路——這道鎖﹐"
			"與那以萬骨煉身的骨屍妖王本是一體﹐須待那妖王伏誅、萬骨崩散之"
			"際﹐方會隨之鬆解而開。且先了結了這頭妖王罷。\n");

	return 1;
}
// vim: set ts=4 sw=4 syntax=lpc

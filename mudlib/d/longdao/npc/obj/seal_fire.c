// seal_fire.c -- 正史主線第六章「四神任務」火神(鳳凰)伏誅所掉的完整神印：火神之印。
//
// 來歷（承接 canon 主線樹 docs 05 L52-54：四神任務 火/天龍/雷/風，誅四神各得一片完
//   整 seal；docs 05 L108 江隕所言——集得火、天龍、雷、風四神的完整封印，以四片神印
//   嵌入星光環，另有一番驚天動地的造化）：
//   火神乃天龍島上以鳳凰之形顯化的烈焰之神，司南方離火、三昧真火。少俠於天龍島誅
//   了這頭烈焰之神，自牠那一蓬不滅的離火中，凝出這一枚通體流轉著赤金火芒的神印——
//   是為四神之印中的『火神之印』。此印須與天龍、雷、風三神之印湊齊，一一嵌入那枚
//   星光環(d/chimei/npc/obj/starlight_ring)的空槽，方成就那了結十三靈的終局大願。
//
// 用法（純任務信物，不另設機制）：
//   * 持此印（與另三神之印），於身上『insert seal of fire』，將此印嵌入星光環的空槽
//     （見 starlight_ring.c::do_insert）。嵌入時此印即沒入環中（destruct）。
//   設 no_sell：神印干係終局任務，玩家須一路帶在身邊，斷不可變賣。
//
// 注意：本印為四神之 die() 逐玩家掉落的任務信物（每位四神-slayer 各掉一枚），不
//   inherit F_UNIQUE（鏡 ghost_ring/starlight_ring 之做法：僅以 set("unique",1) 作風
//   味標記，免多人持印時的任何意外自毀）。

#include <ansi.h>

inherit ITEM;

void create()
{
	set_name(HIR "火神之印" NOR,
		({ "seal of fire", "fire seal", "火神之印" }) );
	set_weight(400);

	if( !clonep() ) {
		set("unit", "枚");
		set("value", 1);
		set("unique", 1);
		set("no_sell", 1);
		set("long",
			"一枚自天龍島火神元神所凝、通體流轉著赤金火芒的神印。印身\n"
			"既非金石、亦非玉璧，而似一團凝而不散的離火所鑄——握之在手，\n"
			"但覺一股灼烈而不灼人的暖意自掌心緩緩漫開，印面隱隱浮著一隻\n"
			"振翅欲飛的鳳凰古紋，紋路間三昧真火明滅流轉，灼灼如活。這便\n"
			"是司南方離火的火神(鳳凰)伏誅之際，自牠那一蓬不滅的離火中凝\n"
			"出的『火神之印』了——一枚元素神之印。傳聞當集齊火、天龍、\n"
			"雷、風四神之印，一一嵌入那枚星光環的空槽(insert seal of\n"
			"fire)，方能成就那了結十三靈、再戰侮天鬼的終局大願。\n");
	}
	setup();
}

// vim: set ts=4 sw=4 syntax=lpc

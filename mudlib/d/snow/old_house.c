// Room: /d/snow/old_house.c

inherit ROOM;

void create()
{
	set("short", "河邊小屋");
	set("long", @LONG
河邊空地旁有一間低矮的小屋﹐屋子雖然簡陋但還算整齊﹐門口
掛著一幅褪色的門簾。屋內陳設簡單﹐一張木桌﹐兩張板凳﹐靠牆擺
著一張窄床﹐床頭放著一盞油燈。桌上攤著幾張泛黃的紙﹐上面寫滿
了密密麻麻的小字﹐似乎是主人的日記或書信。牆上掛著一幅年輕女
子的畫像﹐畫中人眉清目秀﹐看起來不過十六七歲的模樣。
LONG
	);
	set("detail", ([
		"畫像" : "畫中是一個眉清目秀的少女﹐穿著素色衣裳﹐嘴角帶著一絲淡淡的微笑。畫的下方題著兩個小字﹕「小蝶」。\n",
		"書信" : "桌上的信紙已經泛黃﹐字跡潦草﹐隱約可以看到「吾女小蝶」、「音訊全無」、「望歸」等字樣﹐看來是一位老人寫給女兒的信﹐但似乎從未寄出。\n",
		"油燈" : "一盞普通的油燈﹐燈芯已經燒得很短了﹐看來主人節儉慣了。\n",
	]));
	set("exits", ([
		"west" : __DIR__"riverbank",
	]));

	setup();
	replace_program(ROOM);
}

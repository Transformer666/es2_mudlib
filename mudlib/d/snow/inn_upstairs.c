// Room: /d/snow/inn_upstairs.c

inherit ROOM;

void create()
{
    set("short", "客棧二樓雅座");
    set("long", @LONG
這裡是飲風客棧的二樓雅座﹐幾張臨窗的桌子拭得乾乾淨淨﹐
靠窗坐下便可以望見鎮上的廣場﹐樓下大堂的人聲笑語夾著酒菜香
氣﹐順著樓板的縫隙一陣陣透了上來。靠牆一排是幾間客房﹐房門
都虛掩著﹐想是還沒有客人投宿。北邊牆角有一道樓梯通往樓下的
大堂。
LONG
    );
    set("detail", ([ /* sizeof() == 3 */
	"窗" : "推開木窗﹐便可以望見廣場上人來人往﹐廣場中央那棵大榕樹的枝葉在風中輕輕搖晃。",
	"雅座" : "幾張靠窗的桌椅擦拭得一塵不染﹐想是掌櫃留給體面客人用的雅座﹐坐在這裡喝酒聽書﹐比樓下清靜多了。",
	"客房" : "靠牆一排是幾間客房﹐房門都虛掩著﹐裡面被褥疊得整整齊齊﹐看來客棧的生意還算清淡。",
    ]));
    set("no_fight", 1);
    set("exits", ([ /* sizeof() == 1 */
	"down" : __DIR__"inn_hall",
    ]));

    setup();
}

// vim: set ts=4 sw=4 syntax=lpc
